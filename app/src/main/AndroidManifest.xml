<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.test_task.movie_test">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_baseline_movie_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_movie_icon"
        android:supportsRtl="true"
        android:name="com.test_task.movie_test.MovieTestApplication"
        android:theme="@style/Theme.MyApplication" >
        tools:targetApi="31">
        <activity
            android:name="com.test_task.movie_test.MainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>